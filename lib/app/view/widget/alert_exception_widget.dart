import 'package:flutter/material.dart';

class AlertExceptionWidget extends StatelessWidget {
  const AlertExceptionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
