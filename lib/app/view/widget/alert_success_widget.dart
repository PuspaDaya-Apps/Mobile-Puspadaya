import 'package:flutter/material.dart';

class AlertSuccessWidget extends StatelessWidget {
  const AlertSuccessWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
